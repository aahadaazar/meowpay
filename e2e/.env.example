# Copy to .env and fill in. These point the suite at an already-running stack
# (docker compose up --build, per the repo's docker-compose.yml) — Playwright does not
# start the app itself.
E2E_BASE_URL=http://localhost:3000
E2E_BACKEND_URL=http://localhost:8080
